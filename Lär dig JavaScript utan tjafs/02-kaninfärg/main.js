"use strict"

// Variabler
// Lägg till kod här under
// ==>

// Funktioner
function andraFarg() {
  const allaFargerLangd = 7
  const slumptal = Math.floor(Math.random() * allaFargerLangd)
  const allaFargerEn = [
    "yellow",
    "white",
    "pink",
    "lightblue",
    "firebrick",
    "burlywood",
    "darkgrey",
  ]
  const allaFargerSv = ["gul", "vit", "rosa", "ljusblå", "röd", "beige", "grå"]
  const nyFargEn = allaFargerEn[slumptal]
  const nyFargSv = allaFargerSv[slumptal]

  // Lägg till kod här under
  // ==>
}

// Eventlisteners
// Lägg till kod här under
// ==>
